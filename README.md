# Inventory Manager Starter Blueprint (React + Laravel)

This repository contains starter scaffolding for a POS + Inventory system.

## Included
- docs/implementation_blueprint.md
- backend Laravel scaffold (routes, migrations, models, services, tests)
- frontend React scaffold (POS screen, offline queue, theme)

## Next setup
1. Initialize Laravel runtime in backend/laravel
2. Initialize React runtime in frontend/react
3. Run migrations + seeders
